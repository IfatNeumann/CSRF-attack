extends layout

block content
    h1= title
    p Welcome to #{title}
    form(method='POST' action='/login' enctype="plain/text")
        p user name:
        input( type='textbox' name='userName')
        p password:
        input(type='password' name='password2')
        br
        div
            button(type='submit' name='btn' value='Transfer') Transfer
            button(type='submit' value='SafeTransfer' name='btn') SafeTransfer

